# achievements

if the user hasn't unlocked achievements, don't render the animations but still reward it

if the user has, go ahead with animations and such

## Boogie All Night Long

- **id:** `boogie-all-night-long`
- **trigger:** click the disco block on the homepage
